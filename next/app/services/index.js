//TODO : export services from here

// TODO: make api base url not default and edit the way it's
// imported to be { apiBaseUrl } instead of apiBaseUrl
const apiBaseUrl = process.env.NEXT_PUBLIC_NODE_URL
export default apiBaseUrl