/*const { LoginHistory } = require('../models');
const userService = require('./user.service');
const httpStatus = require('http-status');
const ApiError = require('../utils/ApiError');


const createEntry = async (entryBody) => {
  return LoginHistory.create();
};
*/

/*async function addLoginHistory(user_email, login_time, logout_time, session_minutes) {
  try {
  await LoginHistory.create({ //create login history with null values for logout and session time
    //const loginHistory = new LoginHistory({
    user_email,
    login_time,
    logout_time,
    session_minutes,
    });
    //await loginHistory.save();
  } catch (error) {
    console.error('Error adding login history to the database:', error);
  }
}*/

/*module.exports = {
  addLoginHistory
};*/
